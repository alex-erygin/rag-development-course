# Interactive Search Demo - Документация

## 📋 Обзор

`interactive_search.py` - это интерактивный скрипт для тестирования и демонстрации возможностей системы векторного поиска. Скрипт предоставляет удобный командный интерфейс для сравнения семантического и ключевого поиска в реальном времени.

## 🚀 Возможности

- **Интерактивный режим** - работа через командную строку
- **Семантический поиск** - поиск по векторным эмбеддингам
- **Ключевой поиск** - традиционный поиск по точным совпадениям
- **Сравнительный анализ** - одновременное сравнение обоих методов
- **Автоматическая индексация** - создание базы знаний при первом запуске

## 📁 Структура файла

```
interactive_search.py
├── Импорты и зависимости
├── Функция interactive_search()
│   ├── Инициализация системы
│   ├── Проверка существующей базы знаний
│   └── Основной цикл обработки команд
├── Функция main()
└── Точка входа
```

## 🔧 Зависимости

```python
import sys
import os
from vector_search_demo import VectorSearchDemo
from config import *
```

### Внешние зависимости:
- `vector_search_demo.py` - основной модуль системы поиска
- `config.py` - конфигурационный файл
- `chromadb` - векторная база данных
- `openai` - клиент для работы с локальной моделью

## 🎯 Использование

### Запуск скрипта

```bash
python interactive_search.py
```

### Доступные команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `semantic <запрос>` | Семантический поиск | `semantic проблемы с двигателем` |
| `keyword <запрос>` | Поиск по ключевым словам | `keyword температура мотора` |
| `compare <запрос>` | Сравнение обоих методов | `compare безопасность при ремонте` |
| `quit`, `exit`, `q` | Выход из программы | `quit` |

## 📊 Примеры использования

### 1. Семантический поиск

```bash
🎯 Введите команду: semantic проблемы с двигателем
```

**Результат:**
```
🔍 Семантический поиск: 'проблемы с двигателем'

============================================================
РЕЗУЛЬТАТЫ СЕМАНТИЧЕСКОГО ПОИСКА
============================================================

1. Файл: Ремонт двигателя
   Расстояние: 0.1234
   Содержание: При диагностике поломка мотора часто проявляется через нестабильную работу на холостых оборотах и потерю мощности...
```

### 2. Поиск по ключевым словам

```bash
🎯 Введите команду: keyword температура мотора
```

**Результат:**
```
🔍 Поиск по ключевым словам: 'температура мотора'

============================================================
РЕЗУЛЬТАТЫ КЛЮЧЕВОГО ПОИСКА
============================================================

1. Файл: Температурный режим силовой установки
   Релевантность: 0.67 (2 совпадения)
   Содержание: Контроль температуры критически важен для надежной работы двигателя...
```

### 3. Сравнительный анализ

```bash
🎯 Введите команду: compare безопасность при ремонте
```

**Результат:**
```
🔍 Сравнение методов поиска: 'безопасность при ремонте'

📊 СЕМАНТИЧЕСКИЙ ПОИСК:
============================================================
РЕЗУЛЬТАТЫ СЕМАНТИЧЕСКОГО ПОИСКА
============================================================
1. Файл: Техника безопасности
   Расстояние: 0.0891
   Содержание: Работа с автомобильными двигателями требует строгого соблюдения правил безопасности...

📊 ПОИСК ПО КЛЮЧЕВЫМ СЛОВАМ:
============================================================
РЕЗУЛЬТАТЫ КЛЮЧЕВОГО ПОИСКА
============================================================
1. Файл: Техника безопасности
   Релевантность: 1.00 (3 совпадения)
   Содержание: Работа с автомобильными двигателями требует строгого соблюдения правил безопасности...
```

## ⚙️ Конфигурация

Скрипт использует настройки из `config.py`:

```python
# Основные настройки
DEFAULT_SEARCH_RESULTS = 3        # Количество результатов по умолчанию
MAX_CONTENT_PREVIEW_LENGTH = 200  # Длина предварительного просмотра
COLLECTION_NAME = "automotive_knowledge"  # Название коллекции
```

## 🔍 Алгоритм работы

### 1. Инициализация
```python
def interactive_search():
    # Создание экземпляра системы поиска
    search_system = VectorSearchDemo()
    
    # Проверка существующей базы знаний
    try:
        search_system.collection = search_system.client.get_collection(COLLECTION_NAME)
        print("✅ Найдена существующая база знаний")
    except:
        print("📚 Индексируем документы...")
        search_system.index_documents()
```

### 2. Основной цикл обработки команд
```python
while True:
    user_input = input("\n🎯 Введите команду: ").strip()
    
    # Парсинг команды
    parts = user_input.split(' ', 1)
    command, query = parts
    
    # Выполнение соответствующей команды
    if command.lower() == 'semantic':
        # Семантический поиск
    elif command.lower() == 'keyword':
        # Ключевой поиск
    elif command.lower() == 'compare':
        # Сравнительный анализ
```

## 🐛 Обработка ошибок

### Типичные ошибки и их решения

#### 1. Неверный формат команды
```
❌ Неверный формат команды. Пример: 'semantic проблемы с двигателем'
```
**Решение:** Используйте правильный формат: `команда запрос`

#### 2. Ошибка подключения к модели
```
❌ Ошибка: Connection refused
```
**Решение:** Убедитесь, что локальная модель запущена по адресу `http://127.0.0.1:1234`

#### 3. Пустая база знаний
```
📚 Индексируем документы...
```
**Решение:** Скрипт автоматически создаст базу знаний из файлов в директории `KB/`

## 📈 Преимущества интерактивного режима

### 1. **Немедленная обратная связь**
- Результаты поиска отображаются мгновенно
- Возможность быстрого тестирования различных запросов

### 2. **Сравнительный анализ**
- Одновременное сравнение семантического и ключевого поиска
- Визуальное отображение различий в результатах

### 3. **Гибкость использования**
- Интерактивный ввод запросов
- Возможность экспериментирования с различными формулировками

### 4. **Образовательная ценность**
- Демонстрация различий между методами поиска
- Практическое понимание возможностей векторного поиска

## 🔧 Настройка и кастомизация

### Изменение количества результатов
В файле `config.py`:
```python
DEFAULT_SEARCH_RESULTS = 5  # Увеличить количество результатов
```

### Изменение длины предварительного просмотра
```python
MAX_CONTENT_PREVIEW_LENGTH = 300  # Увеличить длину предварительного просмотра
```

### Добавление новых команд
```python
elif command.lower() == 'custom':
    # Ваша новая команда
    custom_results = search_system.custom_search(query)
    search_system.print_search_results(custom_results, "КАСТОМНОГО")
```

## 📊 Метрики и производительность

### Время отклика
- **Семантический поиск**: 1-3 секунды (включая генерацию эмбеддинга)
- **Ключевой поиск**: < 1 секунды
- **Сравнительный анализ**: 2-4 секунды

### Точность поиска
- **Семантический поиск**: Высокая точность для связанных концепций
- **Ключевой поиск**: Точные совпадения, может пропускать синонимы

## 🎓 Обучающие сценарии

### Сценарий 1: Понимание синонимов
```bash
semantic мотор
keyword мотор
compare мотор
```

### Сценарий 2: Контекстный поиск
```bash
semantic поломка
keyword поломка
compare поломка
```

### Сценарий 3: Сложные запросы
```bash
semantic диагностика проблем с охлаждением
keyword диагностика проблем с охлаждением
compare диагностика проблем с охлаждением
```

## 🔗 Интеграция с другими компонентами

### Связь с vector_search_demo.py
- Использует класс `VectorSearchDemo` для основной функциональности
- Наследует все методы поиска и индексации

### Связь с config.py
- Импортирует все конфигурационные параметры
- Использует централизованные настройки

### Связь с test_connection.py
- Рекомендуется запускать `test_connection.py` перед использованием
- Проверка работоспособности модели эмбеддингов

## 📝 Логи и отладка

### Включение подробного логирования
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Отладочная информация
Скрипт выводит:
- Статус подключения к базе данных
- Процесс индексации документов
- Ошибки подключения к модели
- Результаты поиска с метриками

## 🚀 Расширение функциональности

### Добавление новых типов поиска
```python
def hybrid_search(self, query: str, n_results: int = 3):
    """Гибридный поиск: комбинация семантического и ключевого"""
    semantic_results = self.semantic_search(query, n_results)
    keyword_results = self.keyword_search(query, n_results)
    # Логика объединения результатов
    return combined_results
```

### Добавление фильтрации
```python
def filtered_search(self, query: str, filters: dict):
    """Поиск с фильтрами по метаданным"""
    # Реализация фильтрации по дате, типу документа и т.д.
```

### Добавление экспорта результатов
```python
def export_results(self, results: List[Dict], format: str = "json"):
    """Экспорт результатов в различные форматы"""
    # Экспорт в JSON, CSV, PDF и т.д.
```

## 📄 Лицензия

Этот скрипт является частью проекта системы векторного поиска и распространяется под лицензией MIT.

---

**Автор:** Система векторного поиска для демонстрации возможностей RAG  
**Версия:** 1.0.0  
**Дата создания:** 2024  
**Последнее обновление:** 2024 