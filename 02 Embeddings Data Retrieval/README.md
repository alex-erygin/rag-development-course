# Модуль 2: Извлечение данных через эмбеддинги (RAG Pipeline)

## Обзор

Этот модуль демонстрирует полный **RAG (Retrieval-Augmented Generation) pipeline** — от загрузки документов до генерации ответов с помощью LLM. Модуль является основой курса и показывает, как объединить векторный поиск с генеративными моделями для создания интеллектуальной системы вопросов-ответов.

## Концепция RAG

**RAG (Retrieval-Augmented Generation)** — это архитектурный паттерн, который комбинирует:

1. **Retrieval (Извлечение)**: Поиск релевантных документов в базе знаний
2. **Augmentation (Дополнение)**: Обогащение запроса найденной информацией
3. **Generation (Генерация)**: Создание ответа на основе запроса и контекста

## Проблема

Традиционные подходы к поиску информации имеют ограничения:

- **Ключевой поиск**: Находит только точные совпадения слов
- **Чистые LLM**: Ограничены знаниями времени обучения
- **Статические базы**: Сложно обновлять знания модели

## Решение: RAG Pipeline

RAG решает эти проблемы путем:

1. **Семантического поиска**: Понимание смысла запроса, а не только ключевых слов
2. **Динамического контекста**: Предоставление актуальной информации LLM
3. **Трассируемости**: Возможность отследить источники ответов
4. **Масштабируемости**: Легкое добавление новых документов

## Архитектура модуля

```
📄 Документы (Markdown)
    ↓
✂️ Разбиение на чанки (LangChain)
    ↓
🔢 Создание эмбеддингов (Sentence Transformers)
    ↓
🗄️ Хранение в векторной БД (ChromaDB)
    ↓
🔍 Семантический поиск по запросу
    ↓
📋 Формирование контекста
    ↓
🤖 Генерация ответа (LLM)
```

## Структура модуля

```
02 Embeddings Data Retrieval/
├── README.md                    # Документация модуля
├── embeddings_retrieval.py      # Основной скрипт RAG pipeline
├── chroma_db_embeddings/        # Векторная база данных
└── KB/                         # База знаний
    └── report-2024.md          # Тестовый документ (отчет Сбербанка)
```

## Основной файл

### `embeddings_retrieval.py`

**Полный RAG pipeline** с пошаговой демонстрацией:

- 📚 **Загрузка документов**: Чтение Markdown файлов из директории KB
- ✂️ **Чанкинг**: Разбиение на фрагменты по 128 токенов
- 🔢 **Эмбеддинги**: Создание векторных представлений текста
- 🗄️ **Индексация**: Сохранение в ChromaDB с метаданными
- 🔍 **Поиск**: Семантический поиск по запросу пользователя
- 🤖 **Генерация**: Создание ответа через локальную LLM

## Установка и настройка

### 1. Зависимости

Модуль использует следующие библиотеки:

```python
# Основные зависимости
chromadb>=0.4.0           # Векторная база данных
sentence-transformers>=2.2.0  # Создание эмбеддингов
openai>=1.0.0             # Интерфейс для LLM
langchain>=0.1.0          # Разбиение текста на чанки
pypdf>=3.0.0              # Работа с PDF (для расширения)
```

### 2. Настройка локального LLM сервера

**Важно**: Модуль требует запущенный локальный OpenAI-совместимый сервер.

**Настройки по умолчанию:**

- URL: `http://127.0.0.1:1234/v1`
- API ключ: `dummy` (для локальных моделей)
- Модель: `google/gemma-3-12b`

### 3. Структура данных

**База знаний**: Markdown файлы в директории `KB/`
**Векторная БД**: Автоматически создается в `chroma_db_embeddings/`

## Использование

### Базовый запуск

```bash
cd "02 Embeddings Data Retrieval"
python embeddings_retrieval.py
```

### Что происходит при запуске:

1. **📚 Загрузка документов**

   ```
   Загружено 1 документов
   ```

2. **✂️ Разбиение на чанки**

   ```
   Получено 87 чанков
   ```

3. **🗄️ Создание векторной базы**

   ```
   Добавлено 87 чанков в базу данных
   ```

4. **❓ Обработка запроса**

   ```
   Запрос: "Какова чистая прибыль Сбербанка в 2024 году?"
   ```

5. **🔍 Поиск релевантных документов**

   ```
   Найдено 5 наиболее релевантных чанков
   ```

6. **🤖 Генерация ответа**
   ```
   Ответ на основе найденной информации
   ```

## Технические детали

### Конфигурационные параметры

```python
# Основные настройки
KB_DIRECTORY = "./KB"                    # Директория с документами
CHROMA_DB_PATH = "./chroma_db_embeddings" # Путь к векторной БД
COLLECTION_NAME = "knowledge_base_embeddings"  # Имя коллекции
CHUNK_SIZE_TOKENS = 128                   # Размер чанка в токенах
```

### Модель эмбеддингов

**Используется**: `all-MiniLM-L6-v2`

- **Размерность**: 384 измерения
- **Язык**: Многоязычная (включая русский)
- **Скорость**: Быстрая обработка
- **Качество**: Хорошее для большинства задач

### Структура чанков

Каждый чанк содержит:

```python
{
    'content': str,           # Текст чанка
    'source_file': str,       # Имя исходного файла
    'chunk_index': int        # Индекс чанка в документе
}
```

### Метаданные в ChromaDB

```python
{
    'source_file': 'report-2024.md',  # Источник
    'chunk_index': 42                 # Позиция в документе
}
```

## Алгоритм работы

### 1. Подготовка данных

```python
def load_text_files(directory: str) -> list:
    """Загрузка Markdown файлов из директории"""
    # Поиск всех .md файлов
    # Чтение содержимого с обработкой ошибок
    # Возврат списка документов с метаданными
```

### 2. Чанкинг

```python
def split_documents_into_chunks(documents: list, chunk_size: int = 128) -> list:
    """Разбиение документов на чанки фиксированного размера"""
    # Использование SentenceTransformersTokenTextSplitter
    # Разбиение по токенам, а не символам
    # Сохранение метаданных для каждого чанка
```

### 3. Создание векторной базы

```python
def initialize_chromadb():
    """Инициализация ChromaDB с автоматическими эмбеддингами"""
    # Создание персистентного клиента
    # Настройка функции эмбеддингов
    # Создание коллекции с метаданными
```

### 4. Семантический поиск

```python
def search_documents(collection, query: str, n_results: int = 5):
    """Поиск наиболее релевантных документов"""
    # Автоматическое создание эмбеддинга запроса
    # Косинусное сходство с документами
    # Возврат топ-N результатов с метаданными
```

### 5. Генерация ответа

```python
def rag(query, retrieved_documents, model="google/gemma-3-12b"):
    """RAG: объединение поиска и генерации"""
    # Формирование контекста из найденных документов
    # Создание промпта для LLM
    # Генерация ответа на основе контекста
```

## Примеры использования

### Тестовые запросы

**Финансовые показатели:**

```python
query = "Какова чистая прибыль Сбербанка в 2024 году?"
# Ожидаемый ответ: "1 580,3 млрд рублей"
```

**Клиентская база:**

```python
query = "Сколько клиентов у Сбербанка?"
# Ожидаемый ответ: "109,9 млн розничных клиентов"
```

**Технологии:**

```python
query = "Что такое GigaChat?"
# Ожидаемый ответ: Информация о языковой модели Сбера
```

### Анализ результатов поиска

Каждый результат содержит:

```
1. Файл: report-2024.md, Чанк: 15
   Содержание: [Релевантный текст из документа]

2. Файл: report-2024.md, Чанк: 23
   Содержание: [Дополнительная информация]
```

## Конфигурация и настройка

### Изменение размера чанков

```python
# Для коротких документов
CHUNK_SIZE_TOKENS = 64

# Для длинных технических текстов
CHUNK_SIZE_TOKENS = 256

# Для максимального контекста
CHUNK_SIZE_TOKENS = 512
```

### Настройка модели эмбеддингов

```python
# Для русского языка
embedding_function = SentenceTransformerEmbeddingFunction(
    model_name="cointegrated/rubert-tiny2"
)

# Для многоязычных задач
embedding_function = SentenceTransformerEmbeddingFunction(
    model_name="paraphrase-multilingual-MiniLM-L12-v2"
)
```

### Кастомизация промптов

```python
system_prompt = """
Вы - эксперт по [ВАША_ПРЕДМЕТНАЯ_ОБЛАСТЬ].
Отвечайте на вопросы, используя только предоставленную информацию.
Если информации недостаточно, честно об этом сообщите.
"""
```

## Интеграция с другими модулями

### Связь с модулем 1 (Семантический поиск)

- Использует те же принципы векторного поиска
- Добавляет генерацию ответов через LLM
- Демонстрирует практическое применение эмбеддингов

### Основа для модуля 3 (Визуализация)

- Предоставляет готовую векторную базу данных
- Эмбеддинги используются для UMAP визуализации
- Метаданные помогают в анализе результатов

### Платформа для модуля 4 (Query Expansion)

- ChromaDB коллекция переиспользуется
- Базовые функции поиска расширяются
- Архитектура RAG остается неизменной

## Метрики и оценка

### Качество поиска

**Релевантность документов:**

- Проверка соответствия найденных чанков запросу
- Анализ косинусного расстояния
- Оценка покрытия различных аспектов вопроса

**Качество ответов:**

- Фактическая точность
- Полнота информации
- Ссылки на источники

### Производительность

**Время обработки:**

- Создание эмбеддингов: ~1-2 сек для 100 чанков
- Поиск: ~100-200 мс
- Генерация ответа: ~2-5 сек (зависит от LLM)

**Использование памяти:**

- ChromaDB: ~10-50 МБ для типичной базы знаний
- Модель эмбеддингов: ~90 МБ в памяти
- Векторы: ~1.5 КБ на чанк (384 float32)

## Ограничения и рекомендации

### Технические ограничения

**Размер документов:**

- Рекомендуется: до 10 МБ на документ
- Максимум: ограничен доступной памятью
- Оптимально: 1-5 МБ для быстрой обработки

**Качество эмбеддингов:**

- Зависит от предметной области
- Может требовать fine-tuning для специфических задач
- Ограничено контекстным окном модели (512 токенов)

**Локальная LLM:**

- Требует значительные вычислительные ресурсы
- Качество ответов зависит от размера модели
- Необходима настройка для оптимальной производительности

### Рекомендации по использованию

**Подготовка данных:**

- Очистите документы от лишнего форматирования
- Структурируйте информацию логически
- Используйте описательные имена файлов

**Оптимизация поиска:**

- Экспериментируйте с размером чанков
- Настройте количество возвращаемых результатов
- Рассмотрите использование фильтров по метаданным

**Улучшение ответов:**

- Настройте промпты под вашу предметную область
- Используйте примеры в промптах (few-shot learning)
- Добавьте инструкции по формату ответов

## Дальнейшее развитие

### Возможные улучшения

**Расширение форматов:**

```python
# Поддержка PDF
def load_pdf_files(directory: str) -> list:
    # Использование pypdf для извлечения текста

# Поддержка DOCX
def load_docx_files(directory: str) -> list:
    # Использование python-docx
```

**Улучшение чанкинга:**

```python
# Семантическое разбиение
from langchain.text_splitter import RecursiveCharacterTextSplitter

# Разбиение с перекрытием
splitter = RecursiveCharacterTextSplitter(
    chunk_size=256,
    chunk_overlap=50  # Перекрытие для сохранения контекста
)
```

**Гибридный поиск:**

```python
# Комбинация семантического и ключевого поиска
def hybrid_search(collection, query: str, alpha: float = 0.7):
    semantic_results = semantic_search(collection, query)
    keyword_results = keyword_search(collection, query)
    return combine_results(semantic_results, keyword_results, alpha)
```

### Интеграция с внешними системами

**API интерфейс:**

```python
from fastapi import FastAPI

app = FastAPI()

@app.post("/search")
async def search_endpoint(query: str):
    results = search_documents(collection, query)
    return {"results": results}
```

**Веб-интерфейс:**

```python
import streamlit as st

st.title("RAG Search Interface")
query = st.text_input("Введите ваш вопрос:")
if query:
    results = rag_pipeline(query)
    st.write(results)
```

## Заключение

Модуль "02 Embeddings Data Retrieval" представляет собой полную реализацию RAG pipeline и служит основой для понимания современных систем поиска и генерации ответов.

**Ключевые достижения модуля:**

- ✅ Демонстрация полного RAG workflow
- ✅ Интеграция векторного поиска с LLM
- ✅ Практическая работа с реальными данными
- ✅ Основа для дальнейшего изучения RAG-техник

Этот модуль закладывает фундамент для понимания того, как современные AI-системы объединяют поиск информации с генерацией ответов, создавая мощные инструменты для работы с знаниями.
