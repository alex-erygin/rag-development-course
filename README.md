# Система векторного поиска с ChromaDB

Этот проект демонстрирует возможности семантического поиска по сравнению с традиционным поиском по ключевым словам, используя векторную базу данных ChromaDB и локальную модель для генерации эмбеддингов.

## 🚀 Возможности

- **Индексация текстовых файлов** в векторную базу ChromaDB
- **Семантический поиск** с использованием векторных эмбеддингов
- **Поиск по ключевым словам** для сравнения с традиционными методами
- **Интерактивный режим** для тестирования различных запросов
- **Демонстрация различий** между методами поиска

## 📋 Требования

- Python 3.8+
- Локально развернутая модель с OpenAI API (доступная по адресу http://127.0.0.1:1234)
- ChromaDB для векторного хранения

## 🛠️ Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd rag-development-course
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Убедитесь, что локальная модель запущена:**
```bash
# Модель должна быть доступна по адресу http://127.0.0.1:1234
# и поддерживать OpenAI API для эмбеддингов
# Используется модель: text-embedding-nomic-embed-text-v1.5
```

5. **Проверьте конфигурацию:**
```bash
python check_config.py
```

## 📁 Структура проекта

```
rag-development-course/
├── KB/                           # Директория с текстовыми файлами
│   ├── Ремонт двигателя.txt
│   ├── Температурный режим силовой установки.txt
│   └── Техника безопасности.txt
├── config.py                     # Конфигурационный файл
├── vector_search_demo.py         # Основной скрипт демонстрации
├── interactive_search.py         # Интерактивный режим поиска
├── test_connection.py           # Скрипт тестирования подключения
├── check_config.py             # Скрипт проверки конфигурации
├── requirements.txt              # Зависимости Python
├── README.md                    # Этот файл
└── chroma_db/                   # База данных ChromaDB (создается автоматически)
```

## 🎯 Использование

### 1. Проверка конфигурации

Сначала проверьте настройки системы:

```bash
python check_config.py
```

### 2. Тестирование подключения

Проверьте подключение к локальной модели:

```bash
python test_connection.py
```

### 3. Демонстрационный режим

Запустите основной скрипт для автоматической демонстрации:

```bash
python vector_search_demo.py
```

Этот скрипт:
- Индексирует все текстовые файлы из директории `KB/`
- Выполняет демонстрационные запросы
- Показывает сравнение семантического и ключевого поиска

### 2. Интерактивный режим

Для интерактивного тестирования поиска:

```bash
python interactive_search.py
```

Доступные команды:
- `semantic <запрос>` - семантический поиск
- `keyword <запрос>` - поиск по ключевым словам
- `compare <запрос>` - сравнение обоих методов
- `quit` или `exit` - выход

Примеры запросов:
```
semantic проблемы с двигателем
keyword температура мотора
compare безопасность при ремонте
```

## ⚙️ Конфигурация

Все настройки проекта находятся в файле `config.py`:

```python
# Пути к директориям и файлам
KB_DIRECTORY = "KB"                                    # Директория с файлами
CHROMA_DB_PATH = "./chroma_db"                        # Путь к базе данных

# Настройки локальной модели
EMBEDDING_MODEL_ENDPOINT = "http://127.0.0.1:1234/v1" # Адрес локальной модели
EMBEDDING_MODEL_ID = "text-embedding-nomic-embed-text-v1.5"  # Идентификатор модели
EMBEDDING_API_KEY = "dummy"                           # API ключ

# Настройки ChromaDB
COLLECTION_NAME = "automotive_knowledge"               # Название коллекции

# Настройки поиска
DEFAULT_SEARCH_RESULTS = 3                            # Количество результатов по умолчанию
MAX_CONTENT_PREVIEW_LENGTH = 200                      # Максимальная длина предварительного просмотра
```

## 🔍 Как это работает

### Семантический поиск
1. Текст запроса преобразуется в векторный эмбеддинг
2. Система находит наиболее похожие векторы в базе данных
3. Возвращаются документы с наименьшим косинусным расстоянием

### Поиск по ключевым словам
1. Запрос разбивается на отдельные слова
2. Система ищет точные совпадения слов в документах
3. Релевантность вычисляется как процент совпадающих слов

## 📊 Преимущества семантического поиска

1. **Понимание контекста** - находит документы по смыслу, а не только по словам
2. **Синонимы** - понимает, что "мотор" и "двигатель" - это одно и то же
3. **Связанные концепции** - находит документы о "ремонте" при запросе "поломка"
4. **Языковая гибкость** - работает с разными формулировками одного запроса

## 🐛 Устранение неполадок

### Ошибка подключения к модели
```
Ошибка получения эмбеддинга: Connection refused
```
**Решение:** Убедитесь, что локальная модель запущена и доступна по адресу http://127.0.0.1:1234

### Ошибка ChromaDB
```
Ошибка создания коллекции: Collection already exists
```
**Решение:** Скрипт автоматически удаляет существующую коллекцию при создании новой

### Пустая база знаний
```
Текстовые файлы не найдены в директории KB
```
**Решение:** Убедитесь, что в директории `KB/` есть файлы с расширением `.txt`

## 📈 Расширение функциональности

Для добавления новых возможностей:

1. **Новые типы документов** - добавьте поддержку PDF, DOCX и других форматов
2. **Метаданные** - расширьте систему метаданных для лучшей фильтрации
3. **Гибридный поиск** - комбинируйте семантический и ключевой поиск
4. **Веб-интерфейс** - создайте веб-приложение для удобного использования

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

---

**Автор:** Система векторного поиска для демонстрации возможностей RAG
**Версия:** 1.0.0
